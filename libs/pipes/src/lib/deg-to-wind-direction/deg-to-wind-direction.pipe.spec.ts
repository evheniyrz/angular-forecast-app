import { DegToWindDirectionPipe } from './deg-to-wind-direction.pipe';

describe('DegToWindDirectionPipe', () => {
  it('create an instance', () => {
    const pipe = new DegToWindDirectionPipe();
    expect(pipe).toBeTruthy();
  });
});
