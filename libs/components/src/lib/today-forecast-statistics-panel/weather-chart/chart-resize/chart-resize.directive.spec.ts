import { ChartResizeDirective } from './chart-resize.directive';

describe('ChartResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ChartResizeDirective();
    expect(directive).toBeTruthy();
  });
});
